<UserControl x:Class="Led.Views.Controls.MainWindow.NetworkClientOverview"
             Loaded="UserControl_Loaded"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Led.Views"
             xmlns:vm="clr-namespace:Led.ViewModels"
             xmlns:controls="clr-namespace:Led.Views.Controls.MainWindow"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="200">
    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="ITP">
            <StackPanel Orientation="Vertical" />
        </ItemsPanelTemplate>

        <DataTemplate DataType="{x:Type vm:NetworkClientVM}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Label Content="{Binding ID}" Margin="3,3,3,3" Grid.Column="0" />
                <Button Content="{Binding Entity}" Command="{Binding ChangeIDBindingCommand}" Margin="3,3,3,3" Grid.Column="1" />
            </Grid>
        </DataTemplate>

    </UserControl.Resources>
    <Grid>
        
    </Grid>
</UserControl>
