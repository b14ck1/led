<UserControl x:Class="Led.Views.Controls.MainWindow.EffectList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Led.ViewModels"
             xmlns:local="clr-namespace:Led.Views.Controls.MainWindow"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:EffectBaseVM}">
            <Grid>
                <TextBlock Text="{Binding Path=StartFrame, StringFormat=S: {0}}"/>
                <TextBlock Text="{Binding Path=EndFrame, StringFormat=E: {0}}"/>
                <TextBlock Text="{Binding Path=Dauer, StringFormat=D: {0}}"/>
            </Grid>
        </DataTemplate>
        <ItemsPanelTemplate x:Key="ITP">
            <StackPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>
    </UserControl.Resources>
    <Grid>
        <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Hidden">
            <ItemsControl 
                ItemsPanel="{StaticResource ITP}"
                ItemsSource="{Binding EffectList}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
